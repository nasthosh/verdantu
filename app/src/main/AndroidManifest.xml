<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.verdantu">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher_foreground_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity android:name=".activities.RecommendationActivity"></activity>
        <activity android:name=".activities.IntroScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activities.EditActivity" />
        <activity android:name=".activities.UpdateListActivity" />
        <activity android:name=".activities.AddRecipe" />
        <activity android:name=".activities.RecipeActivity" />
        <activity android:name=".activities.AddFoodItems" />
        <activity android:name=".activities.ShowFoods" />
        <activity
            android:name=".activities.HomeScreen"
            android:label="@string/title_activity_home_screen" />
        <activity android:name=".activities.MainActivity"></activity>
    </application>

</manifest>